// Navegação movida para AppLayout.tsx (navbar única no topo).
// Este arquivo mantido apenas para compatibilidade de imports.
export function Sidebar() { return null; }
export function TopNav()   { return null; }